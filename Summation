#include <stdio.h>

int summation(int n){
    
    if(n == 1 || n == 0)
        return 1;
    else
        return n + summation(n-1);
    
}

int main(void){
  int n, num;
  scanf("%d", &n);
  num = summation(n);
  printf("%d", num);

  return 0;
}
